<% @posts.each do |post| %>
	<!-- <h1><%= post.title %></h1>
	<p><%= post.bru_name %></p>
	<p><%= post.body %></p> -->

	<div>
	 <h1><%= link_to post.title, post_path(post) %></h1>
	 <p><%= post.bru_name %></p>
	 <p><%= post.body %></p>
	 <p><%= link_to 'Edit', edit_post_path(post) %></p>
	 <p><%= link_to 'Destroy', post_path(post), method: :delete, data: { 
	 	confirm: "SERIOUSLY?" } %></p>
	 </div>
	<% end %>
